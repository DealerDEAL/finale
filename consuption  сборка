#include <iostream>
#include <fstream>
#include <stdlib.h>
#include <stdio.h>
using namespace std;

class Balance {
    public:
    int balance;
    Balance(int balance) {
        this->balance = balance;
    }
};

    
class User{
    public:
    string name;
    int id, balance;
    User (string name, int id) {
        balance = 5000;
        this->id = id;
        this->name = name;
    }
}; 
    
class Consuption{
    public: 
    int sum;
    int id;
    Consuption (int sum, int userid) {
        this->sum = sum;
        this->id = id;
    }
};
    
int n, i, s;
double x;
int main ()
{
    cout << "ведите количество категорйи трат для 1\n";
    cin >> n;
    int* consuption = (int*)calloc(n, sizeof(int));
    for(i=1; i<n+1; i++){
     cout<<"введите траты месяца "<<i<<":";
    cin>> consuption[i-1];
    };
    for(i=0; i<n; i++){
        
    }
    
    ofstream out;
    out.open("trate1.txt");
    if (out.is_open())
    {
    for(i=1; i<n+1; i++){
    out<<"траты в этом " <<i<< " месеце "<<"= "<<consuption[i-1]<<"$\n";
    }
    }
    out.close();
    
    
    out.open("trate2.txt");
    if (out.is_open())
    {
    double x=0;
    for(int i=0; i<n; i++){
    x=(consuption[i]+0.0)+x;
    }
    double it;
    for(int i=0; i<n; i++){
    s=consuption[i]/x*100;
    out<<"процнты по этому "<<i+1<<" месецу от всех "<<"= "<<s<<"%\n";
    }
    }
    out.close();
    
    return 0;
}
